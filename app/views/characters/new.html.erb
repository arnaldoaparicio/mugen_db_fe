<h2>Submit a new entry for origin</h2>

<%= form_with url: '/characters', method: :post, local: true do |f| %>
  <%= f.label :origin, 'Name of game origin' %>
  <%= f.text_field :origin %>

  <%= f.submit 'Submit' %>
<% end %>
